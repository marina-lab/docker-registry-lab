#################################################
#  Auto-generated kickstart based on hyp3 install
#
#  08-08-2014
#
#  Update net info and root pw before using
#################################################

#version=RHEL7
# System authorization information
auth --enableshadow --passalgo=sha512
# Install OS instead of upgrade
install
# Reboot after installation
reboot
# Use CDROM installation media
cdrom
# Use text mode install
text
# Firewall configuration
firewall --enabled --service=ssh
firstboot --disable
ignoredisk --only-use=sda
# Keyboard layouts
# old format: keyboard us
# new format:
keyboard --vckeymap=us --xlayouts='us'
# System language
lang en_US.UTF-8

# Network information
network  --bootproto=static --device=eth0 --gateway=#.#.#.# --ip=#.#.#.# --nameserver=#.#.#.# --netmask=#.#.#.# --noipv6
network  --bootproto=static --device=eth1 --gateway=#.#.#.# --ip=#.#.#.# --netmask=#.#.#.# --noipv6
network  --hostname=localhost.localdomain
# Root password
rootpw --plaintext XXXXXXXX
# SELinux configuration
selinux --enforcing
# Do not configure the X Window System
skipx
# System timezone
timezone America/Los_Angeles --isUtc
# System bootloader configuration
bootloader --location=mbr --boot-drive=sda
autopart --type=lvm
# Clear the Master Boot Record
zerombr
# Partition clearing information
clearpart --all --initlabel 

%packages
@Core

%end

